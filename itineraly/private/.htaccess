# .htaccess - privateディレクトリ用（Apache 2.4以降）

Require all denied

# Authorizationヘッダーを環境変数にコピー（API認証等で必要なら）
RewriteEngine On
RewriteCond %{HTTP:Authorization} ^(.*)
RewriteRule ^ - [E=HTTP_AUTHORIZATION:%1]